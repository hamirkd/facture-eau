export class Classe {
    id:number
    code:string
    libelle:string 

    constructor(classe){
        this.id = classe.id
        this.code = classe.code
        this.libelle = classe.libelle
    }
}
